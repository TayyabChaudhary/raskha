import { StyleSheet } from "react-native";


export const largeCircleStyles = StyleSheet.create({
    largeCircle: {
        justifyContent: 'center',
        alignItems: 'center',
        position: 'relative',
        marginTop: 100
    },
})